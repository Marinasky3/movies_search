import { create } from '@storybook/theming'
// @ts-ignore
import coreLogo from './static/logo.png'

const isLocal = () => window.location.hostname === 'localhost'

export default create({
  base: 'light',
  brandTitle: 'Core',
  brandUrl: isLocal() ? '/' : 'https://design.procore.com',
  brandImage: coreLogo,
})
