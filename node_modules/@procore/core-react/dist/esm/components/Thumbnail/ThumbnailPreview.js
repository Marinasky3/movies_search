import React from 'react';
import { StyledImageThumbnailPreview } from './Thumbnail.styles';
export function ThumbnailPreview(_a) {
    var file = _a.file;
    var _b = React.useState(''), fileUrl = _b[0], setFileUrl = _b[1];
    React.useEffect(function () {
        var _a;
        if (!file) {
            setFileUrl('');
            return;
        }
        setFileUrl(((_a = URL.createObjectURL(file)) !== null && _a !== void 0 ? _a : '').toString());
    }, [file]);
    React.useEffect(function () {
        return function () {
            if (fileUrl) {
                URL.revokeObjectURL(fileUrl);
            }
        };
    }, [fileUrl]);
    return fileUrl ? React.createElement(StyledImageThumbnailPreview, { "$bgImage": fileUrl }) : null;
}
//# sourceMappingURL=ThumbnailPreview.js.map