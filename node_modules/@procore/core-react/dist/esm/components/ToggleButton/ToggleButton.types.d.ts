import { ButtonAttributes } from '../../utils/types';
import { ButtonSizes } from '../Button/Button.types';
export interface ToggleButtonProps extends ButtonAttributes {
    /**
     * Set the current value of the toggle button
     * @default false
     */
    selected?: boolean;
    /**
     * The icon to display on the left side of the label. This should be an
     * icon from <Code>@procore/core-icons</Code>.
     */
    icon?: string | React.ReactNode;
    /**
     * The toggle button size
     */
    size?: ButtonSizes;
}
