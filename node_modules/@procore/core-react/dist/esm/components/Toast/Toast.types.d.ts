import { Props } from '../../utils/types';
export declare type ToastVariant = 'error' | 'success';
export interface ToastProps extends Props {
    /**
     * Controls icon and background color
     */
    variant: ToastVariant;
}
