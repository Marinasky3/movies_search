import { IconProps } from '../Icon';
export interface TokenProps {
    /**
     * Disables the token
     */
    disabled?: boolean;
    focused?: boolean;
    error?: boolean;
    /**
     * @deprecated
     *
     * The `'disabled'` and `'focused'` variants have been deprecated and will be
     * removed in a future version. Please use the `disabled` and `focused` boolean
     * props instead.
     *
     * **Before**
     *
     * `<Token variant="disabled|focused" />`
     *
     * **After**
     *
     * `<Token disabled focused />`
     *
     * @deprecatedSince 10.20.0
     */
    variant?: 'disabled' | 'focused' | 'icon' | 'standard';
}
/**
 * TODO - This is one of the last places using the old Icon component and
 * should be refactored with the designers
 */
export interface LegacyIconProps extends Omit<IconProps, 'icon'> {
    /**
     * The token icon
     */
    icon: string | React.ReactNode;
}
