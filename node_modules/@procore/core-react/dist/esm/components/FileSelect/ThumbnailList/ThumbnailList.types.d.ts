import { DivAttributes } from '../../../utils/types';
declare type ThumbnailId = string | number;
export interface ThumbnailListItem {
    id: ThumbnailId;
    name: string;
    src?: File | string;
    error?: string;
    response?: unknown;
}
export interface ThumbnailListProps extends Omit<DivAttributes, 'children'> {
    items: ThumbnailListItem[];
    onCancel?: (item: ThumbnailListItem) => void;
    listHeight?: number;
    rowHeight?: number;
}
export {};
