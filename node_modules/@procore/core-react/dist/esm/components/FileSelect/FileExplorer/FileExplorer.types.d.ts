import { FileSelectSource, SourceId } from '../FileSelect.types';
import { ListNavigationApi, ListNavigationConfig } from '../../../state/ListNavigation';
export interface FileExplorerProps {
    sources: FileSelectSource[];
    currentSource: SourceId;
    isOpen: boolean;
    onCurrentSourceChange: (sourceId: SourceId) => void;
    unsubmittedValueCount: number;
    onSubmit?: () => void;
    children: React.ReactNode;
    onModalVisibilityChange: (isOpen: boolean) => void;
    maxFileNumber?: number;
    qa?: {
        attachButton?: string;
        cancelButton?: string;
    };
}
export interface FileExplorerSidebarProps {
    value: SourceId;
    onChange: (selectedSource: SourceId) => void;
    sources: FileSelectSource[];
    isOpen: boolean;
}
export interface SidebarNavigationConfig extends ListNavigationConfig {
    selectedIndex: number;
}
export interface SidebarNavigationApi extends ListNavigationApi {
    up: () => void;
    down: () => void;
}
