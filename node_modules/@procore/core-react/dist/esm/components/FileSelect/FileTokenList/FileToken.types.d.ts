import { ValueId } from '../FileSelect.types';
export interface FileTokenEntry {
    id: string | number;
    name: string;
    error?: string;
    progress?: number;
}
export interface QaTags {
    fileToken?: (id: ValueId) => string;
    closeButton?: string;
}
export interface FileTokenListProps {
    tokens: FileTokenEntry[];
    onClick?: (token: FileTokenEntry) => void;
    onClose?: (token: FileTokenEntry) => void;
    qa?: QaTags;
}
