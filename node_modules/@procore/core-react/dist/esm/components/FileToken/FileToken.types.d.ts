import { Props } from '../../utils/types';
interface QaTags {
    closeButton?: string;
}
export interface FileTokenProps extends Props {
    fileName: string;
    progressValue?: number;
    errorMessage?: string;
    onLabelClick?: () => void;
    onClose?: () => void;
    qa?: QaTags;
}
export {};
