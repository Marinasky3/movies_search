import { Props } from '../../utils/types';
export interface RadioButtonProps extends Props {
    /**
     * Selected state of the radio button
     */
    checked?: boolean;
    /**
     * Disabled state of the radio button
     */
    disabled?: boolean;
    /**
     * Error state of the radio button
     */
    error?: boolean;
}
