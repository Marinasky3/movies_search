export declare type PillColor = 'blue' | 'gray' | 'green' | 'red' | 'yellow';
export interface PillProps extends React.ComponentPropsWithoutRef<'span'> {
    /**
     * Controls the border, background and color of the Pill
     * @defaultValue gray
     */
    color?: PillColor;
}
