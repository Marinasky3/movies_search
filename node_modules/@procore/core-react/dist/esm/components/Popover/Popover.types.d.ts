import { TriggerVariant } from '../../state/Trigger';
import { Placement } from '../../utils/placement';
import { Props } from '../../utils/types';
export interface PopoverContentProps {
    fluid?: boolean;
    placement?: Placement;
}
export interface PopoverProps extends Props {
    /**
     * Whether to show the overlay on initial render
     * @defaultValue false
     */
    initialIsVisible?: boolean;
    /**
     * The tip we want to display
     */
    overlay?: React.ReactElement<{
        placement?: Placement;
    }>;
    /**
     * Determines the position of the arrow
     * @defaultValue top
     */
    placement?: Placement;
    /**
     * The event to trigger the overlay
     * @defaultValue click
     */
    trigger?: TriggerVariant;
}
