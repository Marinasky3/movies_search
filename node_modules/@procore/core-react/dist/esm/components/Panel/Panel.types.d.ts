import { ButtonAttributes, DivAttributes } from '../../utils/types';
import { ButtonProps } from '../Button/Button.types';
export interface PanelQaTags {
    moreIcon?: string;
    title?: string;
}
export interface PanelHeaderProps extends DivAttributes {
    /**
     * Controls visibility and behavior for the "chevron-left" icon on the left
     * side of the title.
     */
    onClickBack?: () => void;
    onClose?: () => void;
}
export interface PanelTitleTooltipProps {
    children: React.ReactNode;
    isVisible: boolean;
    overlay: React.ReactNode;
}
export interface PanelHeaderActionProps extends ButtonAttributes, ButtonProps {
    /**
     * Tooltip/Overflow menu label text.
     * */
    title: string;
}
export interface PanelBodyProps extends DivAttributes {
    /**
     * Applies the gray secondary background color.
     */
    secondaryBgColor?: boolean;
}
export interface PanelContextShape {
    isFooterSticky: boolean;
    setIsFooterSticky: (v: boolean) => void;
}
