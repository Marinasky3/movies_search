import React from 'react';
import { TextEditorProps } from './TextEditor.types';
export declare function TextEditor({ error, init: initOverrides, locale, onChange, onInit: _onInit, plugins, ...props }: TextEditorProps): JSX.Element;
export declare namespace TextEditor {
    var displayName: string;
}
interface ProviderProps {
    children?: React.ReactNode;
}
/**
 * @deprecated
 * The `TextEditorProvider` provider is no longer needed, `<TextEditor />` can be used on its own.
 *
 * **Before**
 *
 * ```
 * <TextEditorProvider>
 *   <TextEditor value="Hello World" />`
 * </TextEditorProvider>
 * ```
 *
 * **After**
 *
 * `<TextEditor value="Hello World" />`
 *
 * @deprecatedSince 10.20.0
 */
export declare function TextEditorProvider({ children }: ProviderProps): JSX.Element;
export default TextEditor;
