import { Props } from '../../utils/types';
import { AvatarIconProps } from '../Avatar/Avatar.types';
export interface ContactItemProps extends Props {
    /**
     * The avatar rich media url
     */
    imageUrl?: string;
    /**
     * The icon to display
     */
    icon?: AvatarIconProps['icon'];
    /**
     * Avatar text label
     */
    initials?: string;
    /**
     * Is the contact item selected
     * @defaultValue false
     */
    selected?: boolean;
    /**
     * Is the contact item disabled
     * @defaultValue false
     */
    disabled?: boolean;
    /**
     * Is the contact item clickable
     * @defaultValue false
     */
    clickable?: boolean;
}
