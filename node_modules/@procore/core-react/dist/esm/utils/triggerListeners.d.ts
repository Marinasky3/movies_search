import { Scope } from '../effects/EventListener';
import { TriggerVariant } from '../state/Trigger';
interface Config {
    value?: boolean;
    off: (event: any) => any;
    on: (event: any) => any;
    trigger: TriggerVariant | TriggerVariant[];
    scope: Scope;
}
export declare function triggerListeners({ value, off, on, trigger, scope }: Config): void;
export {};
