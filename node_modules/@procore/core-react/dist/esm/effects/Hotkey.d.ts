import { Scope } from './EventListener';
declare type EventType = 'keydown' | 'keyup';
interface Config {
    dependencies?: any[];
    event?: EventType;
    handler: (event: KeyboardEvent) => void;
    key: string | string[];
    preventDefault?: boolean;
    scope?: Scope;
    stopPropagation?: boolean;
}
export declare function useHotkey({ scope, ...config }: Config): void;
export {};
