import { Config as EventListenerConfig } from './EventListener';
export interface UseScrollConfig {
    onScroll: EventListenerConfig['handler'];
    scope?: EventListenerConfig['scope'];
}
export declare function useScroll({ onScroll, scope }: UseScrollConfig): void;
