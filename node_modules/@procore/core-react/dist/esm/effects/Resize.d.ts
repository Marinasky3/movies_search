import { Config as EventListenerConfig } from './EventListener';
interface Config extends EventListenerConfig {
    onResize: EventListenerConfig['handler'];
}
export declare function useResize({ onResize, scope, }: Omit<Config, 'event' | 'handler'>): void;
export {};
