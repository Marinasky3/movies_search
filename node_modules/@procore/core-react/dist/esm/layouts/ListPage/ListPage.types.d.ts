import { Props } from '../../utils/types';
export declare type ControlVariant = 'align-right' | 'block' | 'search';
export interface DivProps extends Props {
}
export interface ListPageControlProps extends Props {
    /**
     * Controls whether the contents will align to the right
     */
    alignRight?: boolean;
    /**
     * Controls whether the contents will attmpt to span the full width
     */
    block?: boolean;
    /**
     * Sets width to a specific size for Search to fit
     */
    search?: boolean;
    /**
     * @deprecated please use the equivalent boolean prop instead
     */
    variant?: ControlVariant;
}
