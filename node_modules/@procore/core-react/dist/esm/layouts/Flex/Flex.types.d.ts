import { Props } from '../../utils/types';
import { FlexDirection, FlexOptions, FlexWrap, MarginOptions, PaddingOptions, StyleOptions } from '../Box/Box.types';
export interface FlexProps extends Props, FlexOptions, MarginOptions, PaddingOptions, StyleOptions {
    /**
     * @default 'row'
     */
    direction?: FlexDirection;
    /**
     * @default false
     */
    inline?: boolean;
    flex?: string;
    basis?: string;
    grow?: string;
    shrink?: string;
    tabIndex?: number;
    wrap?: FlexWrap;
    order?: number;
}
