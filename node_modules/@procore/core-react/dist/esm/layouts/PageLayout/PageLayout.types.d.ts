import React from 'react';
import { DivAttributes, Props } from '../../utils/types';
export interface PageComponentProps extends DivAttributes {
    children?: React.ReactNode;
}
export interface PageHeaderProps extends PageComponentProps {
    /**
     * @description If the header should have no background.
     */
    transparent?: boolean;
}
export interface PageAsideProps extends Props, DivAttributes {
    /**
     * @description If the aside block should be open
     */
    open?: boolean;
}
