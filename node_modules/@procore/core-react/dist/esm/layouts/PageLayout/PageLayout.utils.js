export function lookupParentElement(element, dataAttr) {
    var depthLimit = 5;
    var iteration = 0;
    var element_ = element;
    while (iteration++ < depthLimit) {
        var parent_1 = element_.parentElement;
        if (parent_1 === null || parent_1 === void 0 ? void 0 : parent_1.dataset[dataAttr]) {
            return parent_1;
        }
        else if (parent_1) {
            element_ = parent_1;
        }
        else {
            return null;
        }
    }
}
//# sourceMappingURL=PageLayout.utils.js.map