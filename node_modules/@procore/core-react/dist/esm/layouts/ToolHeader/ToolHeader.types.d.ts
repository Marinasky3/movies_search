import { Props } from '../../utils/types';
export declare type ToolHeaderBadgeVariant = 'beta' | 'new' | 'trial';
export interface ToolHeaderBadgeProps extends Props {
    /**
     * Controls the background/border/text colors of the Badge's pill
     */
    variant: ToolHeaderBadgeVariant;
}
export interface ToolHeaderIconProps extends Props {
    /**
     * @deprecated this will take an icon from '@procore/core-icons/dist' package
     * in the near future. See Icon List page for further details
     */
    icon?: string;
}
